const apiService = {
  baseUrl: 'https://localhost:5001/api', //configure con la url de su backend
  token: null,
  /*
  login: async (username, password) => {
    
  },

  getTasks: async () => {
    
  },

  createTask: async (task) => {
    
  },

  updateTask: async (id, task) => {
    
  },

  deleteTask: async (id) => {
   
  } */
};

export default apiService;
